# discord-cache-viewer
Cross-platform app for converting discord cache files into a viewable format.

## Usage

1. Download the latest version for your OS from releases

2. Specify the Discord cache directory, on MacOS it's:
```/Users/<your name>/Library/Application Support/discord/Cache/Cache_Data```

3. Specify the output directory

4. Click "Run conversion"
